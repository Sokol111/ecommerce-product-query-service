logger:
  level: "debug"
  development: true

server:
  port: 8080

mongo:
  host: "localhost"
  port: 27017
  database: "product-query"
  direct-connection: true

kafka:
  brokers: "localhost:9092"
  consumers-config:
    default-group-id: "ecommerce-product-query-service"
    default-auto-offset-reset: "earliest"
    consumers:
      - name: "catalog-events"
        topic: "catalog.product.events"
      - name: "catalog-attribute-events"
        topic: "catalog.attribute.events"

security:
  jwks:
    jwks-url: "http://localhost:3001/oidc/jwks"
    audience: "https://api.sokolshop.com"

observability:
  otel-collector-endpoint: ""
  tracing:
    enabled: true
  metrics:
    enabled: false